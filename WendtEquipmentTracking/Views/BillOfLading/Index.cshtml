@model IEnumerable<BillOfLadingModel>

@{
    ViewBag.Title = "Index";
}

@Html.Partial("ActionNavPartial", ActionNav.Nothing)

<h4>Bill of Ladings</h4>

@if (Html.UserIsInRole(UserRoles.ReadWrite))
{
    <p>
        @Html.ActionLink("Create New BOL", "Create", null, new { @class = "btn btn-sm btn-primary" })
    </p>
}
@if (Model.Count() > 0)
{
    <table class="table table-condensed table-hover table-bordered table-styled my-datatable">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.BillOfLadingNumber)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.DateShipped)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.ShippedFrom)
                </th>
                @if (Html.UserIsInRole(UserRoles.ReadWrite))
                {
                    <th></th>
                }
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.BillOfLadingNumber)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.DateShipped)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.ShippedFrom)
                    </td>
                    @if (Html.UserIsInRole(UserRoles.ReadWrite))
                    {
                        <td>
                            @Html.ActionLink("Edit", "Edit", new { id = item.BillOfLadingId }) |
                            @Html.ActionLink("Details", "Details", new { billOfLadingNumber = item.BillOfLadingNumber }) |
                            @Html.ActionLink("Delete", "Delete", new { id = item.BillOfLadingId })
                        </td>
                    }
                </tr>
            }
        </tbody>
    </table>
}
else
{
    <p class="text-center">No Bill of Ladings Found.</p>
}


@section Scripts {
    @Scripts.Render("~/bundles/table")
}
